import java.sql.SQLOutput;
import java.util.Scanner;

public class ComparingNumbers {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int newNumber = Integer.valueOf(scanner.nextLine());
        int moreNumbers = Integer.valueOf(scanner.nextLine());

        if (newNumber < moreNumbers);{
        System.out.println(newNumber + " is smaller then " + moreNumbers);}

        //else if (newNumber > moreNumbers);{
//            System.out.println(newNumber + " is bigger then " + moreNumbers);
//        }

        }
    }
